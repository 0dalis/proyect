import { AppTheme } from '../theme.types';
import { getTheme } from '../palettes';

// Tema por defecto (índigo claro) — alineado al landing.
export const DefaultTheme: AppTheme = getTheme('indigo', 'light');
