import 'package:flutter/material.dart';

class AppColors {
  // Tu paleta original
  static const Color primaryDark = Color(0xFF2C4CD4);    // Títulos / Botones Principales
  static const Color primaryMedium = Color(0xFF006EEB);  // Iconos activos
  static const Color primaryLight = Color(0xFF0085E8);   // Bordes de enfoque
  static const Color accentBlue = Color(0xFF0096D0);     // Enlaces / "Olvidé contraseña"
  static const Color accentTeal = Color(0xFF00A4AA);     // Fondos suaves / Chips
  static const Color successGreen = Color(0xFF00AF81);   // Confirmaciones / Registro
  // Colores extra para consistencia
  static const Color background = Color(0xFFF5F7FA);     // Fondo de pantalla
  static const Color placeholder = Color(0xFF9E9E9E);    // Texto de ayuda
  static const Color surface = Colors.white;             // Fondo de tarjetas/inputs
}